{
  "$schema": "../node_modules/@tauri-apps/cli/schema/acl-manifests/app.json",
  "identifier": "default",
  "description": "default app capability",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
